{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "provider": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        },
        "model": {
          "type": "string"
        },
        "base_url": {
          "type": "string",
          "description": "Override the provider's API base URL. Empty (default) uses the SDK default endpoint. Use this to route through an OpenAI- or Anthropic-compatible proxy (LiteLLM proxy, Azure OpenAI, on-prem gateway, regional endpoint, ...). For OpenAI-compatible endpoints include the `/v1` path; for Anthropic-compatible endpoints stop at the host root (the SDK appends `/v1/messages`)."
        },
        "api_key_command": {
          "type": "string",
          "description": "Unix only. Shell command run via `/bin/sh -c` whose stdout is the API key. JSON `{\"key\":\"...\",\"expires_at\":\"<RFC3339>\"}` is parsed strictly and memoized to a per-target cache file (early-refreshed via `api_key_refresh_margin`). Plain non-JSON stdout is returned uncached and must be a single non-empty line. On failure ccgate falls through with `kind=credential_unavailable` rather than dropping back to env vars. See README \"Short-lived / rotating API keys\"."
        },
        "api_key_file": {
          "type": "string",
          "description": "Unix only. Absolute path (or `~/`-prefixed) of a file whose contents follow the same shape as `api_key_command` output. Read every hook fire (no caching) so an external rotator can write atomically on its own schedule. Used when `api_key_command` is empty."
        },
        "api_key_refresh_margin": {
          "type": "string",
          "description": "Duration string (Go time.ParseDuration syntax, e.g. \"30s\", \"1m\"). Cache entries are stale once `now + margin >= expires_at`, forcing a helper refresh. Must be `>= 0`; `0s` disables early refresh. Default: 30s. Must be smaller than the helper's TTL or the cache is permanently stale."
        },
        "api_key_command_timeout": {
          "type": "string",
          "description": "Duration string (Go time.ParseDuration syntax). Caps the hot-path cost of one `api_key_command` invocation including flock retries. Must be `> 0` (`0s` is rejected to avoid wedging every hook fire). Default: 5s."
        },
        "timeout_ms": {
          "type": "integer",
          "minimum": 0,
          "description": "API timeout in milliseconds. 0 means no timeout. Default: 20000"
        }
      }
    },
    "allow": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "deny": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "log_path": {
      "type": "string",
      "description": "Log file path. Supports ~ for home directory. Default: $XDG_STATE_HOME/ccgate/<target>/ccgate.log (target = claude / codex)."
    },
    "log_disabled": {
      "type": "boolean",
      "description": "Disable logging entirely. Default: false"
    },
    "log_max_size": {
      "type": "integer",
      "minimum": 0,
      "description": "Max log file size in bytes before rotation. Default: 5242880 (5MB)"
    },
    "metrics_path": {
      "type": "string",
      "description": "Metrics JSONL file path. Default: $XDG_STATE_HOME/ccgate/<target>/metrics.jsonl (target = claude / codex)."
    },
    "metrics_disabled": {
      "type": "boolean",
      "description": "Disable metrics collection. Default: false"
    },
    "metrics_max_size": {
      "type": "integer",
      "minimum": 0,
      "description": "Max metrics file size in bytes before rotation. Default: 2097152 (2MB)"
    },
    "fallthrough_strategy": {
      "type": "string",
      "enum": ["ask", "allow", "deny"],
      "description": "How to resolve LLM-driven fallthrough decisions. 'ask' (default): defer to Claude Code's prompt. 'deny': auto-deny uncertain requests; the hook message tells Claude that the decision was forced for safety and that it must not ask the user or bypass the restriction. 'allow': auto-approve uncertain requests; note that Claude Code's hook spec only delivers decision.message on deny, so Claude will NOT see any warning that an uncertain request was auto-approved — choose this only when you are comfortable with that. Only LLM fallthrough is affected (bypassPermissions, dontAsk, missing API key, non-Anthropic provider, and ExitPlanMode/AskUserQuestion still defer to Claude Code regardless)."
    },
    "environment": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "append_allow": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Allow guidance rules appended on top of the carried-over list (instead of replacing it). Typically used in project-local configs."
    },
    "append_deny": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Deny guidance rules appended on top of the carried-over list."
    },
    "append_environment": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Environment context appended on top of the carried-over list."
    }
  }
}
